import { RangeValueDirective } from './range-value.directive';

describe('RangeValueDirective', () => {
  it('should create an instance', () => {
    const directive = new RangeValueDirective();
    expect(directive).toBeTruthy();
  });
});
